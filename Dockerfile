FROM ghcr.io/linuxserver/code-server

ARG PUID
ARG PGID
ARG TZ
ARG PASSWORD
ARG SUDO_PASSWORD

ENV PUID 1000
ENV PGID 1000
ENV TZ Asia/Tokyo
ENV PASSWORD ${PASSWORD}
ENV SUDO_PASSWORD ${SUDO_PASSWORD}

EXPOSE 8443
